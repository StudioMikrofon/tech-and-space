import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Weather Tracker Pro",
  description: "Living Earth weather mode with cloud bands, wind flow, precipitation, and storms on the shared tracker engine.",
};

export default function WeatherTrackerPage() {
  return null;
}
