import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Space Tracker Pro",
  description: "Shared globe engine in orbital mode with ISS, deep-space objects, and cinematic telemetry.",
};

export default function SpaceTrackerPage() {
  return null;
}
